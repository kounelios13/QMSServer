apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: qms

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - mysql-pvc.yaml
  - mysql-statefulset.yaml
  - mysql-service.yaml
  - qms-deployment.yaml
  - qms-service.yaml
  - ingress.yaml

# Note: clusterissuer.yaml is not namespaced, apply it separately:
# kubectl apply -f k8s/clusterissuer.yaml
